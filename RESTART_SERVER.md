# ⚠️ يجب إعادة تشغيل الخادم!

## المشكلة
```
Unknown API key undefined
```

## السبب
تم إضافة متغيرات Cloudinary إلى `.env` لكن **الخادم لم يُعاد تشغيله**.

Next.js يقرأ متغيرات البيئة عند بدء التشغيل فقط!

---

## ✅ الحل (30 ثانية)

### الخطوة 1: أوقف الخادم
في Terminal، اضغط:
```
Ctrl + C
```

### الخطوة 2: أعد تشغيل الخادم
```bash
npm run dev
```

### الخطوة 3: انتظر حتى يصبح جاهزاً
```
✓ Ready in X.Xs
```

### الخطوة 4: أعد تحميل الصفحة
في المتصفح، اضغط:
```
F5 أو Ctrl + R
```

### الخطوة 5: جرب رفع صورة
✅ يجب أن يعمل الآن!

---

## 🔍 كيف تتأكد من نجاح الحل؟

بعد إعادة التشغيل:

1. افتح: http://localhost:3000/dashboard/games/new
2. اذهب للخطوة 4
3. اضغط "رفع صورة من الكمبيوتر"

**إذا رأيت:**
- ✅ نافذة Cloudinary تفتح = نجح!
- ❌ رسالة خطأ حمراء = راجع الخطوات أدناه

---

## 🔧 إذا ما زال لا يعمل

### تحقق من:

#### 1. متغيرات البيئة موجودة؟
```bash
Get-Content .env | Select-String "CLOUDINARY"
```

يجب أن ترى:
```
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=c-648de07d1227d0885badf850d0b7e5
CLOUDINARY_API_KEY=577559934418585
CLOUDINARY_API_SECRET=-U52_OeSyuZBFjSqJ_XRaV-Yl9c
```

#### 2. الخادم يعمل؟
تحقق من Terminal:
```
✓ Ready in X.Xs
Local: http://localhost:3000
```

#### 3. Upload Preset موجود؟
- افتح: https://console.cloudinary.com/settings/upload
- تأكد من وجود preset باسم: `traditional_games`
- تأكد من Signing Mode: `Unsigned`

---

## 📝 ملاحظات مهمة

### متى يجب إعادة تشغيل الخادم؟

يجب إعادة التشغيل عند:
- ✅ إضافة متغيرات جديدة إلى `.env`
- ✅ تعديل متغيرات موجودة في `.env`
- ✅ حذف متغيرات من `.env`

لا يحتاج إعادة تشغيل عند:
- ❌ تعديل ملفات الكود (`.tsx`, `.ts`)
- ❌ تعديل ملفات CSS
- ❌ إضافة ملفات جديدة

### لماذا؟

Next.js يقرأ ملف `.env` **مرة واحدة فقط** عند بدء التشغيل.

---

## 🎯 الخلاصة

```
المشكلة: Unknown API key undefined
السبب: الخادم لم يُعاد تشغيله بعد إضافة المتغيرات
الحل: Ctrl+C ثم npm run dev
الوقت: 30 ثانية
```

---

## 🚀 بعد إعادة التشغيل

ستحصل على:
- ✅ رفع من الكمبيوتر
- ✅ رفع من الكاميرا
- ✅ رفع من URL
- ✅ واجهة عربية كاملة
- ✅ 25GB تخزين مجاني

---

**الحالة:** ✅ الحل بسيط جداً  
**الوقت:** 30 ثانية فقط  
**الإجراء:** أعد تشغيل الخادم الآن!
