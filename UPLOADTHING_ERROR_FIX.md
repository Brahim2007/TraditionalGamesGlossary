# حل خطأ: Missing token ⚠️

## الخطأ
```
UploadThingError: Missing token. Please set the `UPLOADTHING_TOKEN` environment variable
```

---

## السبب
متغير البيئة `UPLOADTHING_TOKEN` غير موجود في ملف `.env`

---

## الحل السريع ⚡

### الخطوة 1: افتح ملف `.env`
في جذر المشروع، افتح (أو أنشئ) ملف `.env`

### الخطوة 2: أضف المتغير

```env
UPLOADTHING_TOKEN=your_secret_key_here
```

**استبدل `your_secret_key_here` بالمفتاح السري من Uploadthing**

### الخطوة 3: احصل على المفتاح

1. اذهب إلى: https://uploadthing.com/dashboard
2. اختر تطبيقك (أو أنشئ واحد جديد)
3. اذهب إلى **API Keys** أو **Settings**
4. انسخ **Secret Key** (يبدأ عادة بـ `sk_live_` أو `sk_test_`)

### الخطوة 4: أعد تشغيل الخادم

```bash
# أوقف الخادم (Ctrl+C)
# ثم شغله مرة أخرى
npm run dev
```

---

## مثال على ملف `.env` كامل

```env
# Database
DATABASE_URL="postgres://..."

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment
NODE_ENV="development"

# Uploadthing (رفع الصور)
UPLOADTHING_TOKEN=sk_live_xxxxxxxxxxxxxxxxxxxxx
```

---

## ملاحظات مهمة 📝

### 1. تغيير في الإصدار الجديد
في الإصدارات الحديثة من Uploadthing (v7+):
- ❌ `UPLOADTHING_SECRET` (قديم)
- ❌ `UPLOADTHING_APP_ID` (قديم)
- ✅ `UPLOADTHING_TOKEN` (جديد)

### 2. أنواع المفاتيح
- `sk_test_...` - للتطوير والاختبار
- `sk_live_...` - للإنتاج

### 3. الأمان
- ⚠️ لا تشارك المفتاح السري مع أحد
- ⚠️ لا تضف ملف `.env` إلى Git
- ⚠️ تأكد من وجود `.env` في `.gitignore`

---

## البديل المؤقت 🔄

إذا لم ترغب في إعداد Uploadthing الآن:

### استخدم خيار "أضف رابط صورة"

1. في صفحة إضافة لعبة جديدة
2. اذهب إلى الخطوة 4 (الوسائط والمجتمع)
3. استخدم القسم الأزرق "أو أضف رابط صورة"
4. ارفع صورتك إلى [Imgur](https://imgur.com/)
5. انسخ الرابط المباشر والصقه

**هذا الحل يعمل فوراً بدون إعداد!**

---

## التحقق من نجاح الإصلاح ✓

بعد إضافة `UPLOADTHING_TOKEN` وإعادة تشغيل الخادم:

1. افتح: http://localhost:3000/dashboard/games/new
2. اذهب إلى الخطوة 4
3. اضغط "رفع صورة من الكمبيوتر"
4. اختر صورة

**النتيجة المتوقعة:**
- ✅ يبدأ الرفع بدون أخطاء
- ✅ تظهر رسالة "جاري الرفع..."
- ✅ تظهر الصورة بعد اكتمال الرفع

---

## استكشاف الأخطاء 🔧

### المشكلة: الخطأ ما زال موجوداً
**الحل:**
1. تأكد من كتابة `UPLOADTHING_TOKEN` بشكل صحيح (حساس لحالة الأحرف)
2. تأكد من عدم وجود مسافات قبل أو بعد المفتاح
3. تأكد من إعادة تشغيل الخادم بعد تعديل `.env`

### المشكلة: المفتاح غير صحيح
**الحل:**
1. تحقق من أنك نسخت المفتاح كاملاً
2. تأكد من أنك تستخدم **Secret Key** وليس **Public Key**
3. جرب إنشاء مفتاح جديد من لوحة التحكم

### المشكلة: لا يوجد لدي حساب Uploadthing
**الحل:**
استخدم خيار "أضف رابط صورة" كبديل مؤقت (راجع أعلاه)

---

## روابط مفيدة 🔗

- [Uploadthing Dashboard](https://uploadthing.com/dashboard)
- [Uploadthing Documentation](https://docs.uploadthing.com/)
- [Environment Variables in Next.js](https://nextjs.org/docs/app/building-your-application/configuring/environment-variables)

---

**تم التحديث:** 31 يناير 2026  
**الحالة:** ✅ موثق ومختبر
