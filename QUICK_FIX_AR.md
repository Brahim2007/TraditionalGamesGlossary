# 🔧 حل سريع للخطأ

## المشكلة
```
Error: Cannot read properties of undefined (reading 'findMany')
```

هذا يعني أن Prisma Client لم يتم تحديثه بعد لإضافة نموذج Category.

---

## ✅ الحل (3 خطوات)

### 1️⃣ أوقف خادم التطوير
في Terminal حيث يعمل `npm run dev`:
- اضغط `Ctrl + C`
- انتظر حتى يتوقف تماماً

---

### 2️⃣ أعد توليد Prisma Client
```bash
npx prisma generate
```

**المخرجات المتوقعة:**
```
✔ Generated Prisma Client
```

---

### 3️⃣ طبق تحديثات قاعدة البيانات
```bash
npx prisma db push
```

**المخرجات المتوقعة:**
```
✔ Database synchronized with Prisma schema
```

---

### 4️⃣ أضف البيانات الافتراضية
```bash
node scripts/apply-category-migration.js
```

**المخرجات المتوقعة:**
```
✅ Created "الكل"
✅ Created "ألعاب حركية"
...
✅ Migration completed!
📊 Created: 7 categories
```

---

### 5️⃣ أعد تشغيل الخادم
```bash
npm run dev
```

---

## 🎯 الآن جرب

افتح المتصفح: http://localhost:3000

يجب أن تعمل الصفحة الرئيسية بدون أخطاء وترى التصنيفات!

---

## 📝 ملخص الأوامر (نسخ ولصق)

```bash
# أوقف الخادم أولاً (Ctrl+C)

# ثم نفذ هذه الأوامر:
npx prisma generate
npx prisma db push
node scripts/apply-category-migration.js
npm run dev
```

---

## 🆘 إذا استمر الخطأ EPERM

إذا ظهر خطأ "EPERM: operation not permitted":

1. **أغلق VS Code تماماً**
2. **أغلق أي Terminal مفتوح**
3. **افتح VS Code مرة أخرى**
4. **افتح Terminal جديد**
5. **جرب الأوامر مرة أخرى**

أو:

```bash
# امسح مجلد node_modules/.prisma
rm -rf node_modules/.prisma
# أو على Windows:
rmdir /s /q node_modules\.prisma

# ثم أعد التوليد
npx prisma generate
```

---

**السبب:** الخادم كان يستخدم ملفات Prisma، لذا لم يستطع تحديثها.  
**الحل:** إيقاف الخادم أولاً قبل التحديث.

✅ **هذا يجب أن يحل المشكلة!**
