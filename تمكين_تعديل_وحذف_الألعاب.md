# 🎮 تمكين تعديل وحذف الألعاب

## ✅ تم التنفيذ بنجاح!

تم إضافة وظائف تعديل وحذف الألعاب في جميع الأماكن المطلوبة.

## 📁 الملفات المُنشأة/المُعدلة

### 1. صفحة تعديل اللعبة ✅
**الملف**: `app/dashboard/games/edit/[id]/page.tsx`

**الميزات**:
- ✅ نموذج متعدد الخطوات (4 خطوات) مثل صفحة الإضافة
- ✅ تحميل بيانات اللعبة تلقائياً
- ✅ تحديث جميع الحقول
- ✅ زر حذف مع تأكيد
- ✅ التحقق من الصلاحيات
- ✅ رسائل نجاح/خطأ واضحة

**الخطوات**:
1. **الخطوة 1**: البيانات الأساسية (الاسم، الدولة، نوع اللعبة، الوصف)
2. **الخطوة 2**: المشاركون والبيئة (الفئة العمرية، الممارسون، الأدوات)
3. **الخطوة 3**: آلية اللعب (القواعد، نظام الفوز/الخسارة)
4. **الخطوة 4**: الوسائط والمجتمع (السياق الاجتماعي، المراجع)

### 2. API Endpoint ✅
**الملف**: `app/api/games/[id]/route.ts`

**الوظائف**:
- ✅ GET: الحصول على بيانات اللعبة
- ✅ التحقق من المصادقة
- ✅ التحقق من الصلاحيات
- ✅ تحميل جميع العلاقات (country, heritageField, tags, media, references)

**الصلاحيات**:
- **Admin**: يمكنه تعديل أي لعبة
- **Reviewer**: يمكنه تعديل الألعاب المنشورة وقيد المراجعة
- **Editor**: يمكنه تعديل ألعابه فقط

### 3. مكون إجراءات اللعبة ✅
**الملف**: `components/dashboard/GameActions.tsx`

**الميزات**:
- ✅ زر عرض اللعبة
- ✅ زر تعديل (حسب الصلاحيات)
- ✅ زر حذف (حسب الصلاحيات)
- ✅ تأكيد قبل الحذف
- ✅ حالة تحميل أثناء الحذف
- ✅ تحديث الصفحة بعد الحذف

**الصلاحيات**:
- **عرض**: الجميع
- **تعديل**: Admin, Reviewer, Editor (ألعابه فقط)
- **حذف**: Admin, Editor (ألعابه فقط)

### 4. تحديث صفحة قائمة الألعاب ✅
**الملف**: `app/dashboard/games/page.tsx`

**التعديلات**:
- ✅ استخدام مكون `GameActions` بدلاً من الأزرار المنفصلة
- ✅ عرض زر الحذف للمستخدمين المصرح لهم
- ✅ تمرير جميع البيانات المطلوبة للمكون

## 🚀 كيفية الاستخدام

### 1. تعديل لعبة

#### من صفحة قائمة الألعاب:
1. انتقل إلى: `http://localhost:3001/dashboard/games`
2. ابحث عن اللعبة المطلوبة
3. انقر على زر "تعديل"
4. قم بتعديل الحقول المطلوبة
5. انقر على "حفظ التعديلات"

#### مباشرة من URL:
```
http://localhost:3001/dashboard/games/edit/[GAME_ID]
```

### 2. حذف لعبة

#### من صفحة قائمة الألعاب:
1. انتقل إلى: `http://localhost:3001/dashboard/games`
2. ابحث عن اللعبة المطلوبة
3. انقر على أيقونة سلة المهملات 🗑️
4. أكد الحذف في النافذة المنبثقة

#### من صفحة التعديل:
1. افتح صفحة تعديل اللعبة
2. انقر على زر "حذف اللعبة" (أحمر في الأعلى)
3. أكد الحذف

## 🔒 الصلاحيات

### Admin (مدير)
- ✅ عرض جميع الألعاب
- ✅ تعديل أي لعبة
- ✅ حذف أي لعبة

### Reviewer (مراجع)
- ✅ عرض الألعاب المنشورة وقيد المراجعة
- ✅ تعديل الألعاب المنشورة وقيد المراجعة
- ❌ لا يمكنه الحذف

### Editor (محرر)
- ✅ عرض ألعابه والألعاب المنشورة
- ✅ تعديل ألعابه فقط
- ✅ حذف ألعابه فقط

### Viewer (مستعرض)
- ✅ عرض الألعاب المنشورة فقط
- ❌ لا يمكنه التعديل
- ❌ لا يمكنه الحذف

## ⚠️ ملاحظات مهمة

### 1. الحذف هو أرشفة
عند حذف لعبة، لا يتم حذفها فعلياً من قاعدة البيانات، بل يتم:
- تغيير حالتها إلى `archived`
- إخفاؤها من العرض العام
- الاحتفاظ بها للسجلات التاريخية

```typescript
// في lib/actions/game.ts
await db.game.update({
  where: { id: gameId },
  data: {
    reviewStatus: ReviewStatus.archived
  }
})
```

### 2. رسالة التأكيد
قبل الحذف، تظهر رسالة تأكيد:
```
⚠️ هل أنت متأكد من حذف اللعبة "[اسم اللعبة]"؟

سيتم أرشفة اللعبة ولن تكون مرئية للجمهور.
هذا الإجراء لا يمكن التراجع عنه.
```

### 3. سجل المراجعة
يتم تسجيل جميع الإجراءات في جدول `ReviewLog`:
- **created**: عند إنشاء اللعبة
- **updated**: عند تحديث اللعبة
- **archived**: عند حذف/أرشفة اللعبة

## 🎯 الأماكن التي تعمل فيها الوظائف

### 1. صفحة قائمة الألعاب ✅
`http://localhost:3001/dashboard/games`
- زر تعديل لكل لعبة
- زر حذف لكل لعبة (حسب الصلاحيات)

### 2. صفحة تعديل اللعبة ✅
`http://localhost:3001/dashboard/games/edit/[id]`
- نموذج تعديل كامل
- زر حذف في الأعلى

### 3. صفحة عرض اللعبة (مستقبلاً)
`http://localhost:3001/game/[slug]`
- يمكن إضافة أزرار تعديل/حذف للمستخدمين المصرح لهم

### 4. صفحة المراجعة (مستقبلاً)
`http://localhost:3001/dashboard/review`
- يمكن إضافة أزرار تعديل/حذف

## 🔧 الاختبار

### 1. اختبار التعديل

```bash
# 1. تشغيل الخادم
npm run dev

# 2. تسجيل الدخول
# http://localhost:3001/login

# 3. الانتقال إلى قائمة الألعاب
# http://localhost:3001/dashboard/games

# 4. اختيار لعبة والنقر على "تعديل"

# 5. تعديل بعض الحقول

# 6. حفظ التعديلات

# 7. التحقق من التحديث في قاعدة البيانات
npm run db:studio
```

### 2. اختبار الحذف

```bash
# 1. من صفحة قائمة الألعاب
# انقر على أيقونة سلة المهملات

# 2. أكد الحذف

# 3. تحقق من أن اللعبة اختفت من القائمة

# 4. تحقق في Prisma Studio
# يجب أن تكون حالة اللعبة = "archived"
npm run db:studio
```

### 3. اختبار الصلاحيات

```bash
# 1. سجل الدخول كـ Editor
# حاول تعديل لعبة لمستخدم آخر
# يجب أن تظهر رسالة خطأ

# 2. سجل الدخول كـ Viewer
# لا يجب أن تظهر أزرار التعديل/الحذف

# 3. سجل الدخول كـ Admin
# يجب أن تستطيع تعديل/حذف أي لعبة
```

## 📊 مخطط التدفق

### تعديل لعبة:
```
المستخدم → صفحة قائمة الألعاب
         ↓
      زر "تعديل"
         ↓
   صفحة التعديل (تحميل البيانات)
         ↓
   تعديل الحقول
         ↓
   "حفظ التعديلات"
         ↓
   updateGame() → قاعدة البيانات
         ↓
   رسالة نجاح → العودة للقائمة
```

### حذف لعبة:
```
المستخدم → صفحة قائمة الألعاب
         ↓
      أيقونة 🗑️
         ↓
   رسالة تأكيد
         ↓
   تأكيد الحذف
         ↓
   deleteGame() → تغيير الحالة إلى "archived"
         ↓
   رسالة نجاح → تحديث الصفحة
```

## 🐛 استكشاف الأخطاء

### خطأ: "غير مصرح بالوصول"
**الحل**: تأكد من تسجيل الدخول

### خطأ: "ليس لديك صلاحية لتعديل هذه اللعبة"
**الحل**: تحقق من دورك وملكية اللعبة

### خطأ: "فشل تحميل بيانات اللعبة"
**الحل**: تحقق من أن معرف اللعبة صحيح

### خطأ: "فشل التحديث"
**الحل**: تحقق من صحة البيانات المدخلة

## ✨ التحسينات المستقبلية

1. ✅ إضافة تأكيد قبل مغادرة الصفحة إذا كانت هناك تعديلات غير محفوظة
2. ✅ إضافة معاينة قبل الحفظ
3. ✅ إضافة سجل التغييرات (diff) لعرض ما تم تعديله
4. ✅ إضافة إمكانية استعادة الألعاب المؤرشفة
5. ✅ إضافة تعديل سريع (inline editing) في القائمة
6. ✅ إضافة حذف متعدد (bulk delete)

## 🎉 الخلاصة

الآن يمكنك:
- ✅ تعديل أي لعبة (حسب الصلاحيات)
- ✅ حذف/أرشفة الألعاب (حسب الصلاحيات)
- ✅ عرض الألعاب من أي مكان
- ✅ التحكم الكامل في الألعاب من لوحة التحكم

جميع الوظائف تعمل في:
- ✅ `http://localhost:3001/dashboard/games`
- ✅ `http://localhost:3001/dashboard/games/edit/[id]`
- ✅ أي مكان آخر يستخدم مكون `GameActions`

---

**تم التحديث**: 2026-01-31
**الحالة**: ✅ جاهز للاستخدام
