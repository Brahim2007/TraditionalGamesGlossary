# إعداد نظام رفع الصور (Uploadthing)

## المشكلة الحالية ❌

عند محاولة رفع صورة في صفحة `/dashboard/games/new`، يظهر خطأ 500:

```
POST /api/uploadthing?actionType=upload&slug=gameImage 500 in 817ms
```

## السبب 🔍

**متغيرات البيئة الخاصة بـ Uploadthing مفقودة** من ملف `.env`

النظام يحتاج إلى:
- `UPLOADTHING_SECRET`
- `UPLOADTHING_APP_ID`

## الحل ✅

### الخطوة 1: إنشاء حساب Uploadthing

1. انتقل إلى: https://uploadthing.com/
2. قم بإنشاء حساب جديد أو تسجيل الدخول
3. أنشئ تطبيق جديد (New App)

### الخطوة 2: الحصول على المفاتيح

بعد إنشاء التطبيق، ستحصل على:
- **App ID**: معرف التطبيق
- **Secret Key**: المفتاح السري

### الخطوة 3: إضافة المفاتيح إلى ملف `.env`

أضف السطور التالية إلى ملف `.env`:

```env
# Uploadthing (رفع الصور)
UPLOADTHING_TOKEN=your_secret_key_here
```

**ملاحظة مهمة:** 
- في الإصدارات الجديدة من Uploadthing، تم تغيير اسم المتغير من `UPLOADTHING_SECRET` إلى `UPLOADTHING_TOKEN`
- استخدم نفس المفتاح السري (Secret Key) الذي حصلت عليه من لوحة التحكم
- لم يعد `UPLOADTHING_APP_ID` مطلوباً في الإصدارات الحديثة

### الخطوة 4: إعادة تشغيل الخادم

```bash
# أوقف الخادم (Ctrl+C)
# ثم شغله مرة أخرى
npm run dev
```

## التحقق من التكوين ✓

بعد إضافة المفاتيح، يمكنك التحقق من أن النظام يعمل بشكل صحيح:

```bash
node scripts/test-image-upload.js
```

يجب أن ترى:
```
✅ جميع الملفات المطلوبة موجودة
✅ نظام رفع الصور تم تكوينه بشكل صحيح
✅ صفحة إضافة لعبة جديدة تحتوي على مكون رفع الصور
✅ إجراءات الخادم تدعم حفظ الصور
🎉 نظام رفع الصور جاهز للاستخدام!
```

## بديل مؤقت (للتطوير فقط) 🚧

إذا كنت تريد اختبار النظام بدون Uploadthing، يمكنك:

1. استخدام حقل إدخال URL بدلاً من رفع الملفات
2. استخدام خدمة مجانية مثل Imgur أو Cloudinary
3. تخزين الصور محلياً في مجلد `public/uploads`

## الملفات المتأثرة 📁

- `lib/uploadthing.ts` - تكوين Uploadthing
- `app/api/uploadthing/route.ts` - API endpoint
- `components/ui/upload-button.tsx` - مكون رفع الصور
- `app/dashboard/games/new/page.tsx` - صفحة إضافة لعبة جديدة

## ملاحظات مهمة ⚠️

1. **لا تشارك المفاتيح السرية**: لا تضف ملف `.env` إلى Git
2. **الحد الأقصى لحجم الملف**: 4MB
3. **عدد الملفات المسموح**: حتى 5 صور لكل لعبة
4. **أنواع الملفات المدعومة**: صور فقط (JPEG, PNG, GIF, WebP)

## روابط مفيدة 🔗

- [Uploadthing Documentation](https://docs.uploadthing.com/)
- [Uploadthing Dashboard](https://uploadthing.com/dashboard)
- [Next.js App Router Guide](https://docs.uploadthing.com/getting-started/appdir)
