# 🗄️ تحديث قاعدة البيانات - دليل كامل

## ✅ تم الإصلاح بنجاح!

تم إصلاح مشكلة تشغيل `db:seed` وإضافة قسم التطابقات (Game Similarities) إلى ملف seed.

## 🔧 المشاكل التي تم حلها

### 1️⃣ خطأ في سكريبت db:seed

**المشكلة:**
```
SyntaxError: Expected property name or '}' in JSON at position 1
```

**السبب:**
صيغة JSON خاطئة في `package.json`:
```json
"db:seed": "npx ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
```

**الحل:**
```json
"db:seed": "tsx prisma/seed.ts"
```

### 2️⃣ قسم التطابقات مفقود

**المشكلة:**
ملف `prisma/seed.ts` لم يكن يحتوي على بيانات تجريبية للتطابقات (Game Similarities).

**الحل:**
تمت إضافة قسمين جديدين:

#### أ) Game Concepts (المفاهيم الثقافية)
```typescript
const concept1 = await prisma.gameConcept.create({
  data: {
    name: 'ألعاب المنقلة والحفر',
    description: 'مجموعة من الألعاب التقليدية...',
    createdById: adminContributor.id
  }
})
```

#### ب) Game Similarities (التطابقات)
تم إنشاء 4 أمثلة للتطابقات بحالات مختلفة:
- ✅ **Accepted** (مقبول): تطابق مؤكد بين لعبتين
- ⏳ **Pending** (قيد المراجعة): يحتاج مراجعة من المنسق
- ❌ **Rejected** (مرفوض): ليس تطابقاً حقيقياً
- ⏸️ **Postponed** (مؤجل): يحتاج مزيد من البحث

## 📊 البيانات المُضافة

### Game Concepts (2)
1. **ألعاب المنقلة والحفر**
   - وصف: ألعاب تعتمد على الحفر في الأرض ونقل الحصى
   
2. **ألعاب الكرات والرمي**
   - وصف: ألعاب تعتمد على رمي الكرات نحو أهداف محددة

### Game Similarities (4)

#### 1. تطابق مقبول (Accepted) ✅
- **النتيجة الإجمالية**: 0.85
- **النتيجة البنيوية**: 0.90
- **النتيجة الدلالية**: 0.80
- **نتيجة التراث**: 0.85
- **نتيجة AI**: 0.83
- **الحالة**: مقبول ومرتبط بمفهوم ثقافي
- **ملاحظات المنسق**: "ألعاب متشابهة في الآلية والأدوات"

#### 2. تطابق قيد المراجعة (Pending) ⏳
- **النتيجة الإجمالية**: 0.72
- **الحالة**: في انتظار مراجعة المنسق
- **أسباب التشابه**: ألعاب حركية، تعتمد على السرعة

#### 3. تطابق مرفوض (Rejected) ❌
- **النتيجة الإجمالية**: 0.65
- **الحالة**: مرفوض
- **ملاحظات المنسق**: "الألعاب مختلفة في الجوهر"

#### 4. تطابق مؤجل (Postponed) ⏸️
- **النتيجة الإجمالية**: 0.78
- **الحالة**: مؤجل
- **ملاحظات المنسق**: "يحتاج المزيد من البحث والتوثيق"

## 🚀 كيفية الاستخدام

### 1. تثبيت المتطلبات
```bash
npm install
```

### 2. تشغيل Migrations
```bash
npm run db:migrate
```

### 3. تعبئة قاعدة البيانات
```bash
npm run db:seed
```

### 4. فتح Prisma Studio للمعاينة
```bash
npm run db:studio
```

ثم افتح المتصفح على: `http://localhost:5555`

## 📋 البيانات المُضافة في Seed

| الجدول | العدد | الوصف |
|--------|-------|-------|
| Countries | 22 | جميع الدول العربية |
| Heritage Fields | 6 | مجالات التراث (UNESCO) |
| Tags | 17 | وسوم التصنيف |
| Games | 10 | ألعاب تجريبية |
| Media | 10 | صور ووسائط |
| References | 10 | مراجع ومصادر |
| Review Logs | 3 | سجلات المراجعة |
| **Game Concepts** | **2** | **المفاهيم الثقافية** |
| **Game Similarities** | **4** | **التطابقات** |

## 🔍 كيفية التحقق من التطابقات

### في Prisma Studio:
1. افتح جدول `GameSimilarity`
2. يجب أن تجد 4 سجلات
3. تحقق من الحقول:
   - `overallScore`: النتيجة الإجمالية
   - `status`: الحالة (accepted, pending, rejected, postponed)
   - `explanation`: شرح التشابه (JSON)

### في Dashboard:
1. شغل الخادم: `npm run dev`
2. افتح: `http://localhost:3000/dashboard/matches`
3. يجب أن تظهر التطابقات الـ 4

## 📝 بنية بيانات التطابق

```typescript
{
  gameAId: string,           // معرف اللعبة الأولى
  gameBId: string,           // معرف اللعبة الثانية
  overallScore: number,      // النتيجة الإجمالية (0-1)
  structuralScore: number,   // النتيجة البنيوية
  semanticScore: number,     // النتيجة الدلالية
  heritageScore: number,     // نتيجة التراث
  aiScore: number,           // نتيجة AI
  algorithm: string,         // الخوارزمية المستخدمة
  isAiAssisted: boolean,     // هل استخدم AI؟
  status: enum,              // الحالة
  curatorId: string,         // معرف المنسق
  conceptId: string,         // معرف المفهوم الثقافي
  curatorNotes: string,      // ملاحظات المنسق
  reviewedAt: DateTime,      // تاريخ المراجعة
  explanation: JSON          // شرح مفصل
}
```

## 🎯 حالات التطابق (Similarity Status)

| الحالة | الوصف | الإجراء المطلوب |
|--------|-------|-----------------|
| **pending** | في انتظار المراجعة | يحتاج مراجعة من المنسق |
| **accepted** | مقبول | تم ربطه بمفهوم ثقافي |
| **rejected** | مرفوض | ليس تطابقاً حقيقياً |
| **postponed** | مؤجل | يحتاج مزيد من البحث |

## 🔧 أوامر مفيدة

```bash
# إعادة تعيين قاعدة البيانات بالكامل
npm run db:reset

# فتح Prisma Studio
npm run db:studio

# تشغيل migrations فقط
npm run db:migrate

# تعبئة البيانات فقط
npm run db:seed

# تعبئة المستخدمين التجريبيين
npm run db:seed-users

# توليد Prisma Client
npm run db:generate
```

## 📊 الملفات المعدلة

1. ✅ `package.json` - إصلاح سكريبت db:seed
2. ✅ `prisma/seed.ts` - إضافة قسم التطابقات
3. ✅ `تحديث_قاعدة_البيانات.md` - هذا الملف

## 🎉 النتيجة

الآن قاعدة البيانات تحتوي على:
- ✅ جميع البيانات الأساسية (دول، مجالات تراث، وسوم)
- ✅ 10 ألعاب تجريبية
- ✅ 2 مفاهيم ثقافية
- ✅ 4 تطابقات بحالات مختلفة
- ✅ وسائط ومراجع وسجلات مراجعة

يمكنك الآن:
1. ✅ إضافة ألعاب جديدة
2. ✅ عرض التطابقات في Dashboard
3. ✅ مراجعة التطابقات وقبولها/رفضها
4. ✅ ربط الألعاب بمفاهيم ثقافية

## 🚨 ملاحظات مهمة

### 1. ترتيب معرفات الألعاب
في جدول `GameSimilarity`، يجب أن يكون `gameAId < gameBId` أبجدياً لضمان التفرد.

### 2. شرح التطابق (Explanation)
حقل `explanation` هو JSON يحتوي على:
```json
{
  "reasons": ["سبب 1", "سبب 2"],
  "differences": ["اختلاف 1", "اختلاف 2"]
}
```

### 3. المفاهيم الثقافية
يمكن ربط عدة ألعاب بنفس المفهوم الثقافي عبر حقل `conceptId` في جدول `Game`.

## 🔄 الخطوات التالية

1. ✅ تشغيل seed - **مكتمل**
2. ✅ التحقق من البيانات في Prisma Studio - **مكتمل**
3. ⏳ اختبار صفحة التطابقات في Dashboard
4. ⏳ اختبار إضافة لعبة جديدة
5. ⏳ اختبار حساب التطابهات التلقائي

---

**تم التحديث**: 2026-01-31
**الحالة**: ✅ جاهز للاستخدام
