# ⚡ تطبيق سريع - خطوتان فقط

## المشكلة
جدول `Category` غير موجود في قاعدة البيانات بعد.

## الحل - خطوتان

### الخطوة 1️⃣: إنشاء الجدول
```bash
npx prisma db push
```

**ماذا يفعل؟**
- يقرأ ملف `schema.prisma`
- ينشئ جدول `Category` في قاعدة البيانات
- يحدث Prisma Client

**المخرجات المتوقعة:**
```
✔ Generated Prisma Client
✔ Database synchronized with Prisma schema
```

---

### الخطوة 2️⃣: إضافة البيانات الافتراضية
```bash
node scripts/apply-category-migration.js
```

**ماذا يفعل؟**
- يضيف 7 تصنيفات افتراضية
- يتحقق من عدم التكرار
- يعرض النتائج

**المخرجات المتوقعة:**
```
🚀 Starting Category migration...
✅ Category table already exists
📊 Found 0 existing categories
✅ Created "الكل"
✅ Created "ألعاب حركية"
✅ Created "ألعاب ذهنية"
...
✅ Migration completed!
```

---

## إعادة تشغيل الخادم

```bash
npm run dev
```

---

## ✅ التحقق

1. **افتح الصفحة الرئيسية:**
   http://localhost:3000
   - يجب أن ترى التصنيفات

2. **افتح لوحة التحكم:**
   http://localhost:3000/dashboard/categories
   - يجب أن ترى 7 تصنيفات

---

## 🆘 إذا ظهرت مشاكل

### خطأ: "Environment variable not found: DATABASE_URL"
**الحل:**
```bash
# تأكد من وجود ملف .env
# وأنه يحتوي على:
DATABASE_URL="postgresql://..."
```

### خطأ: "Can't reach database server"
**الحل:**
- تأكد من أن قاعدة البيانات شغالة
- تحقق من صحة DATABASE_URL

### خطأ: "relation Category already exists"
**الحل:**
- هذا جيد! الجدول موجود بالفعل
- انتقل مباشرة للخطوة 2

---

**هذا كل شيء!** خطوتان فقط 🎉
