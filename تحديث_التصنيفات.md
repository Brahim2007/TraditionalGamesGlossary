# تحديث نظام التصنيفات - إدارة التصنيفات من لوحة التحكم

## 📋 نظرة عامة

تم تحديث نظام التصنيفات في الصفحة الرئيسية ليصبح ديناميكياً ويمكن إدارته بالكامل من لوحة التحكم بدلاً من كونه ثابتاً في الكود.

## ✨ الميزات الجديدة

### 1. التصنيفات الديناميكية
- ✅ التصنيفات يتم جلبها من قاعدة البيانات
- ✅ تظهر التصنيفات النشطة فقط في الصفحة الرئيسية
- ✅ يمكن ترتيب التصنيفات حسب الأولوية
- ✅ دعم الألوان المخصصة لكل تصنيف
- ✅ اختيار من مجموعة متنوعة من الأيقونات

### 2. لوحة التحكم الكاملة
- ✅ إضافة تصنيفات جديدة
- ✅ تعديل التصنيفات الموجودة
- ✅ حذف التصنيفات (للمدراء فقط)
- ✅ إخفاء/إظهار التصنيفات
- ✅ إعادة ترتيب التصنيفات
- ✅ معاينة مباشرة للأيقونة واللون

### 3. الصلاحيات
- **المراجعون والمدراء**: إضافة وتعديل وإخفاء/إظهار التصنيفات
- **المدراء فقط**: حذف التصنيفات نهائياً

## 🗄️ البنية الجديدة

### قاعدة البيانات
تم إضافة جدول `Category` بالحقول التالية:
- `id`: معرف فريد
- `name`: اسم التصنيف بالعربية
- `slug`: رابط التصنيف (URL-friendly)
- `description`: وصف التصنيف (اختياري)
- `icon`: اسم الأيقونة من Lucide Icons
- `order`: ترتيب العرض
- `isActive`: حالة النشاط (ظاهر/مخفي)
- `color`: لون مخصص (اختياري)

### الملفات الجديدة

1. **Schema Update**: `prisma/schema.prisma`
   - إضافة نموذج Category

2. **Migration**: `prisma/migrations/002_add_category_table.sql`
   - ملف SQL لإنشاء جدول التصنيفات
   - يتضمن البيانات الافتراضية

3. **Server Actions**: `lib/actions/category.ts`
   - `getCategories()` - جلب جميع التصنيفات
   - `getActiveCategories()` - جلب التصنيفات النشطة فقط
   - `getCategoryById()` - جلب تصنيف محدد
   - `createCategory()` - إضافة تصنيف جديد
   - `updateCategory()` - تحديث تصنيف
   - `deleteCategory()` - حذف تصنيف
   - `toggleCategoryStatus()` - تفعيل/إخفاء تصنيف
   - `reorderCategories()` - إعادة ترتيب التصنيفات

4. **Dashboard Pages**:
   - `app/dashboard/categories/page.tsx` - صفحة إدارة التصنيفات (Server Component)
   - `app/dashboard/categories/CategoriesClient.tsx` - واجهة إدارة التصنيفات (Client Component)

5. **Updated Components**:
   - `components/public/CategoryGrid.tsx` - محدث لجلب التصنيفات من قاعدة البيانات
   - `app/(public)/page.tsx` - محدث لتمرير التصنيفات من API
   - `app/dashboard/DashboardSidebar.tsx` - إضافة رابط صفحة التصنيفات

## 🚀 كيفية الاستخدام

### تطبيق التحديثات على قاعدة البيانات

```bash
# تشغيل ملف SQL مباشرة على قاعدة البيانات
psql -U your_username -d your_database -f prisma/migrations/002_add_category_table.sql

# أو استخدام Prisma Studio
npx prisma studio

# ثم تشغيل السكريبت يدوياً
```

### الوصول إلى لوحة التحكم

1. سجل دخول كمراجع أو مدير
2. انتقل إلى لوحة التحكم
3. اضغط على "التصنيفات" في القائمة الجانبية
4. ستظهر لك جميع التصنيفات الموجودة

### إضافة تصنيف جديد

1. اضغط على "إضافة تصنيف جديد"
2. أدخل اسم التصنيف (مثل: ألعاب حركية)
3. سيتم توليد الرابط (slug) تلقائياً أو يمكنك تعديله
4. أضف وصف اختياري
5. اختر الأيقونة من القائمة المتاحة
6. اختر لون مخصص
7. حدد إذا كان التصنيف نشطاً
8. اضغط "إضافة"

### تعديل تصنيف

1. اضغط على زر "تعديل" في بطاقة التصنيف
2. عدل البيانات المطلوبة
3. اضغط "تحديث"

### إخفاء/إظهار تصنيف

- اضغط على زر "إخفاء" لإخفاء التصنيف من الصفحة الرئيسية
- اضغط على زر "تفعيل" لإظهاره مرة أخرى
- التصنيفات المخفية لن تظهر للزوار لكن لن يتم حذفها

### حذف تصنيف (للمدراء فقط)

- اضغط على أيقونة سلة المهملات
- أكد الحذف
- **تحذير**: الحذف نهائي ولا يمكن التراجع عنه

## 🎨 الأيقونات المتاحة

- LayoutGrid (الشبكة)
- Activity (النشاط)
- Brain (العقل)
- Award (الجائزة)
- Anchor (المرساة)
- ScrollText (اللفافة)
- Smile (الابتسامة)
- Users (المستخدمون)
- Heart (القلب)
- Trophy (الكأس)
- Target (الهدف)
- Sparkles (البريق)
- Star (النجمة)

## 📊 التصنيفات الافتراضية

تم إضافة 7 تصنيفات افتراضية:

1. **الكل** - عرض جميع الألعاب
2. **ألعاب حركية** - ألعاب تعتمد على الحركة والنشاط البدني
3. **ألعاب ذهنية** - ألعاب تعتمد على التفكير والذكاء
4. **تراث خليجي** - ألعاب من التراث الخليجي
5. **ألعاب بحرية** - ألعاب مرتبطة بالبحر والساحل
6. **تراث عراقي** - ألعاب من التراث العراقي
7. **ألعاب طريفة** - ألعاب مسلية وطريفة

## 🔧 التفاصيل التقنية

### التحقق من الصلاحيات

جميع عمليات التصنيفات تتحقق من صلاحيات المستخدم:
- القراءة: متاحة للجميع
- الإضافة/التعديل/الإخفاء: مراجعون ومدراء
- الحذف: مدراء فقط

### إعادة التحميل التلقائي

عند أي تعديل على التصنيفات، يتم إعادة تحميل:
- الصفحة الرئيسية (`/`)
- صفحة المعرض (`/gallery`)
- صفحة إدارة التصنيفات (`/dashboard/categories`)

### التحقق من البيانات

استخدام Zod للتحقق من البيانات:
- الاسم: حرفين على الأقل
- الرابط: حروف إنجليزية صغيرة وأرقام وشرطات فقط
- الأيقونة: مطلوبة
- الترتيب: رقم صحيح غير سالب

## 🐛 استكشاف الأخطاء

### التصنيفات لا تظهر في الصفحة الرئيسية

1. تحقق من أن التصنيف **نشط** (isActive = true)
2. تحقق من أن قاعدة البيانات محدثة
3. تحقق من السجلات (logs) للأخطاء

### لا يمكن إضافة تصنيف

1. تحقق من أنك مسجل دخول كمراجع أو مدير
2. تحقق من عدم وجود تصنيف بنفس الاسم
3. تحقق من عدم وجود تصنيف بنفس الرابط (slug)

### خطأ في قاعدة البيانات

```bash
# إعادة توليد Prisma Client
npx prisma generate

# التحقق من حالة قاعدة البيانات
npx prisma db pull

# تطبيق التحديثات
npx prisma db push
```

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطي**: يُنصح بعمل نسخة احتياطية من قاعدة البيانات قبل تطبيق التحديثات
2. **الترتيب**: يتم ترتيب التصنيفات حسب حقل `order` تصاعدياً
3. **الروابط**: الروابط (slugs) يجب أن تكون فريدة ومتوافقة مع URLs
4. **الألوان**: استخدم ألوان بصيغة Hex (مثل: #3B7C5F)

## 🎯 المميزات المستقبلية المقترحة

- [ ] إعادة ترتيب التصنيفات بالسحب والإفلات (Drag & Drop)
- [ ] رفع صور مخصصة للتصنيفات بدلاً من الأيقونات
- [ ] إحصائيات عدد الألعاب في كل تصنيف
- [ ] تصدير/استيراد التصنيفات
- [ ] نسخ التصنيفات

## 📞 الدعم

في حال واجهت أي مشاكل:
1. تحقق من هذا الملف
2. راجع سجلات الأخطاء في Console
3. تحقق من صلاحيات المستخدم
4. تأكد من تطبيق جميع التحديثات على قاعدة البيانات
