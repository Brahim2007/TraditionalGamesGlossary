# تحديث صفحة المراجعة - Review Page Update

## ✅ التحديثات المنفذة

تم تحديث صفحة المراجعة `/dashboard/review` لعرض بيانات حقيقية من قاعدة البيانات بدلاً من البيانات الوهمية.

### 1. إنشاء API Routes

#### `/api/review` (GET)
- جلب جميع الألعاب التي حالتها `under_review`
- جلب إحصائيات المراجعة (الموافق عليها، المرفوضة، قيد المراجعة)
- يتطلب صلاحيات `reviewer` أو `admin`

#### `/api/review/[id]` (PATCH)
- تنفيذ إجراءات المراجعة:
  - `approve`: الموافقة على اللعبة ونشرها
  - `reject`: رفض اللعبة
  - `revision`: إرجاع اللعبة للمساهم للتعديل
- تسجيل جميع الإجراءات في `ReviewLog` و `SystemLog`

### 2. تحديث صفحة المراجعة

#### الميزات الجديدة:
- ✅ جلب البيانات الحقيقية من API
- ✅ عرض معلومات كاملة عن كل لعبة:
  - الاسم الرسمي
  - الدولة
  - المساهم والمؤسسة
  - نوع اللعبة
  - المجال التراثي
  - الوصف
  - وقت الإرسال (منذ كم ساعة/يوم)
- ✅ إحصائيات حقيقية:
  - عدد الألعاب قيد المراجعة
  - عدد الموافق عليها هذا الأسبوع
  - عدد المرفوضة هذا الأسبوع
- ✅ إجراءات المراجعة:
  - معاينة اللعبة
  - الموافقة والنشر
  - طلب تعديلات
  - الرفض مع ملاحظات
- ✅ إشعارات نجاح/فشل العمليات (Toast notifications)
- ✅ حالة تحميل أثناء جلب البيانات
- ✅ تعطيل الأزرار أثناء تنفيذ الإجراءات

### 3. نظام الإشعارات (Toast)

تم إنشاء نظام إشعارات بسيط:
- `hooks/use-toast.ts`: Hook لإدارة الإشعارات
- `components/ui/toaster.tsx`: مكون عرض الإشعارات
- إخفاء تلقائي بعد 5 ثوانٍ
- دعم نوعين: نجاح وخطأ

### 4. سكريبت للاختبار

تم إنشاء `scripts/set-game-for-review.js` لتعيين لعبة للمراجعة بسهولة:

```bash
node scripts/set-game-for-review.js
```

## 🔗 الروابط

- **صفحة المراجعة**: http://localhost:3004/dashboard/review
- **API المراجعة**: http://localhost:3004/api/review

## 📝 ملاحظات

1. **الصلاحيات**: فقط المستخدمون بدور `reviewer` أو `admin` يمكنهم الوصول لصفحة المراجعة
2. **حالة اللعبة**: الصفحة تعرض فقط الألعاب بحالة `under_review`
3. **الإحصائيات**: تحسب الإحصائيات للأسبوع الأخير فقط
4. **السجلات**: جميع إجراءات المراجعة يتم تسجيلها في `ReviewLog` و `SystemLog`

## 🎯 الحالات المدعومة

### حالات اللعبة (ReviewStatus):
- `draft`: مسودة (لم يتم إرسالها للمراجعة بعد)
- `under_review`: قيد المراجعة (تظهر في صفحة المراجعة)
- `published`: منشورة (تمت الموافقة عليها)
- `rejected`: مرفوضة
- `archived`: مؤرشفة

### إجراءات المراجعة:
1. **الموافقة**: تحول الحالة إلى `published` وتضيف تاريخ النشر
2. **الرفض**: تحول الحالة إلى `rejected` مع إضافة ملاحظات المراجع
3. **طلب تعديل**: تحول الحالة إلى `draft` لإعادتها للمساهم

## 🧪 الاختبار

1. قم بتشغيل السكريبت لإضافة لعبة للمراجعة:
   ```bash
   node scripts/set-game-for-review.js
   ```

2. افتح صفحة المراجعة:
   ```
   http://localhost:3004/dashboard/review
   ```

3. جرب الإجراءات المختلفة:
   - معاينة اللعبة
   - الموافقة
   - طلب تعديل (مع إضافة ملاحظات)
   - الرفض (مع إضافة ملاحظات)

## 📊 البيانات المعروضة

لكل لعبة قيد المراجعة:
- **الاسم**: `canonicalName`
- **الدولة**: `country.name`
- **المساهم**: `contributor.name` + `contributor.institution`
- **نوع اللعبة**: `gameType`
- **المجال التراثي**: `heritageField.name`
- **الوصف**: `description`
- **وقت الإرسال**: محسوب من `createdAt`

## 🔄 سير العمل

1. المساهم يضيف لعبة جديدة (حالة: `draft`)
2. المساهم يرسل اللعبة للمراجعة (حالة: `under_review`)
3. المراجع يفتح صفحة المراجعة ويرى اللعبة
4. المراجع يتخذ أحد القرارات:
   - **موافقة** → حالة: `published` (تظهر في الموقع العام)
   - **رفض** → حالة: `rejected` (لا تظهر)
   - **طلب تعديل** → حالة: `draft` (يعود للمساهم للتعديل)

## ✨ التحسينات المستقبلية المقترحة

- [ ] إضافة فلترة وبحث في قائمة المراجعة
- [ ] إضافة ترتيب حسب التاريخ/الأولوية
- [ ] إضافة معاينة سريعة (modal) بدلاً من الانتقال لصفحة أخرى
- [ ] إضافة إشعارات بريد إلكتروني للمساهمين عند اتخاذ قرار
- [ ] إضافة تاريخ كامل للمراجعة لكل لعبة
- [ ] إضافة إمكانية تعيين مراجع محدد لكل لعبة
