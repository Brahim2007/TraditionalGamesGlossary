# تنظيف قاعدة البيانات قبل النشر على Vercel

## 📋 الوصف

هذا السكريبت يحذف جميع الألعاب الوهمية والبيانات المرتبطة بها من قاعدة البيانات، مع الإبقاء على:
- ✅ التصنيفات (Categories)
- ✅ الدول (Countries)
- ✅ المجالات التراثية (HeritageFields)
- ✅ الوسوم (Tags)
- ✅ المستخدمين/المديرين (Contributors)
- ✅ الجلسات (Sessions)
- ✅ الإعدادات (Settings)
- ✅ اقتراحات الألعاب (GameSuggestions)

## 🗑️ البيانات التي سيتم حذفها

- جميع الألعاب (Games)
- جميع الوسائط المرتبطة (Media)
- جميع المراجع (References)
- جميع روابط الوسوم (GameTags)
- جميع سجلات المراجعة (ReviewLogs)
- جميع علاقات التشابه (GameSimilarities)
- جميع المفاهيم المرتبطة (GameConcepts)

## 🚀 طريقة الاستخدام

### 1. التأكد من الاتصال بقاعدة البيانات

تأكد من أن متغير البيئة `DATABASE_URL` مضبوط بشكل صحيح في ملف `.env`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/database_name"
```

### 2. تشغيل السكريبت

```bash
npm run db:clean-games
```

أو مباشرة:

```bash
npx tsx scripts/clean-dummy-games.ts
```

### 3. التحقق من النتائج

بعد تشغيل السكريبت، سترى:
- عدد السجلات المحذوفة
- عدد السجلات المحفوظة (التصنيفات والمستخدمين)

## ⚠️ تحذيرات مهمة

1. **هذه العملية لا يمكن التراجع عنها!** تأكد من عمل نسخة احتياطية من قاعدة البيانات قبل التشغيل.

2. **لا تشغل هذا السكريبت في بيئة الإنتاج** إلا إذا كنت متأكداً تماماً من أنك تريد حذف جميع الألعاب.

3. **تأكد من أنك في بيئة التطوير** قبل التشغيل.

## 💾 عمل نسخة احتياطية

قبل تشغيل السكريبت، يُنصح بعمل نسخة احتياطية:

```bash
# باستخدام pg_dump (PostgreSQL)
pg_dump -U username -d database_name > backup_$(date +%Y%m%d_%H%M%S).sql

# أو باستخدام Prisma Studio للتحقق من البيانات
npm run db:studio
```

## ✅ بعد التنظيف

بعد تشغيل السكريبت بنجاح:
1. قاعدة البيانات جاهزة للنشر على Vercel
2. جميع التصنيفات والبيانات الأساسية محفوظة
3. يمكن للمديرين تسجيل الدخول بشكل طبيعي
4. يمكن البدء بإضافة ألعاب جديدة من خلال لوحة التحكم

## 🔄 استعادة البيانات (إذا لزم الأمر)

إذا كنت بحاجة لاستعادة البيانات:

```bash
# استعادة من النسخة الاحتياطية
psql -U username -d database_name < backup_file.sql

# أو إعادة تشغيل seed (سيضيف الألعاب الوهمية مرة أخرى)
npm run db:seed
```

## 📝 ملاحظات

- السكريبت يحذف فقط البيانات المرتبطة بالألعاب
- جميع التصنيفات والبيانات الأساسية محفوظة
- بيانات المستخدمين والمديرين محفوظة بالكامل
- يمكن إضافة ألعاب جديدة بعد التنظيف مباشرة
