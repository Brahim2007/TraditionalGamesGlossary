# 🎮 دليل اختبار حفظ الألعاب

## ✅ تم إصلاح المشكلة!

تم حل مشكلة عدم حفظ الألعاب في قسم "إضافة لعبة جديدة". الآن النظام يعمل بشكل صحيح!

## 🚀 خطوات الاختبار السريعة

### 1️⃣ تجهيز قاعدة البيانات

```bash
# تشغيل migrations
npm run db:migrate

# تعبئة البيانات الأساسية (الدول، مجالات التراث، إلخ)
npm run db:seed
```

### 2️⃣ تشغيل الخادم

```bash
npm run dev
```

### 3️⃣ تسجيل الدخول

افتح المتصفح على: `http://localhost:3000/login`

استخدم أحد الحسابات التجريبية:
- **البريد**: `admin@traditionalgames.org`
- **كلمة المرور**: (إذا لم تكن قد أنشأت حساباً، قم بإنشاء حساب جديد)

### 4️⃣ إضافة لعبة جديدة

1. انتقل إلى: `http://localhost:3000/dashboard/games/new`
2. املأ الحقول التالية:

#### الخطوة 1: البيانات الأساسية
- **اسم اللعبة**: "الركض بالحاجبين المرفوعين"
- **الدولة**: اختر "دولة قطر"
- **نوع اللعبة**: اختر "ألعاب طريفة"
- **الوصف**: "تمرين في فصل الحواس يكسر الفطرة البشرية التي تميل لتقطيب الجبين عند الركض"

#### الخطوة 2: المشاركون والبيئة
- **الفئة العمرية**: "9 - 12 سنة"
- **نوع الممارسين**: "مختلط"
- **عدد اللاعبين**: "3 - 10 لاعبين"
- **الأدوات**: "لا يوجد"
- **المكان**: "السكيك أو الحوي"
- **الوقت**: "النهار"

#### الخطوة 3: القواعد
- **القاعدة 1**: "يصطف المتسابقون عند خط البداية"
- **القاعدة 2**: "رفع الحواجب لأقصى حد قبل الانطلاق"
- **القاعدة 3**: "الركض بسرعة قصوى دون إنزال الحاجبين"

#### الخطوة 4: الوسائط
- **السياق الاجتماعي**: "ضبط النفس والذكاء العاطفي"
- **المراجع**: "سلسلة الألعاب الشعبية القطرية - كتارا"

3. ضع علامة ✓ على "أقر بصحة البيانات"
4. انقر على "حفظ وإرسال للمراجعة"

### 5️⃣ التحقق من النجاح

يجب أن تظهر رسالة:
```
✅ تم إنشاء اللعبة بنجاح كمسودة!

يمكنك الآن مراجعتها ونشرها من قسم "الألعاب".
```

ثم يتم إعادة توجيهك إلى: `http://localhost:3000/dashboard/games`

### 6️⃣ التحقق من قاعدة البيانات

افتح Prisma Studio:
```bash
npm run db:studio
```

ثم:
1. افتح جدول `Game`
2. يجب أن تجد اللعبة الجديدة بحالة `draft`
3. تحقق من جميع الحقول المحفوظة

## 📋 الحقول المطلوبة (لا يمكن الحفظ بدونها)

✅ **اسم اللعبة** (3-200 حرف)
✅ **الدولة** (اختيار من القائمة)
✅ **نوع اللعبة** (اختيار من القائمة)
✅ **الوصف** (50-5000 حرف)
✅ **قاعدة واحدة على الأقل** (10-1000 حرف)

## 🔧 استكشاف الأخطاء

### ❌ "غير مصرح بالوصول"
**الحل**: سجل الدخول أولاً

### ❌ "يرجى اختيار دولة صحيحة"
**الحل**: شغل `npm run db:seed`

### ❌ "بيانات غير صالحة"
**الحل**: تأكد من ملء جميع الحقول المطلوبة

### ❌ خطأ في الاتصال بقاعدة البيانات
**الحل**: 
1. تأكد من تشغيل PostgreSQL
2. تحقق من ملف `.env` وأن `DATABASE_URL` صحيح
3. شغل `npm run db:migrate`

## 🎯 ما الذي تم إصلاحه؟

### قبل الإصلاح ❌
- النموذج لا يستدعي أي دالة حفظ
- البيانات لا تُحفظ في قاعدة البيانات
- رسالة نجاح وهمية فقط

### بعد الإصلاح ✅
- استدعاء دالة `createGame()` الفعلية
- حفظ البيانات في قاعدة البيانات
- تحويل الحقول بشكل صحيح
- الحصول على معرفات الدول والمجالات تلقائياً
- إنشاء الوسوم ومجالات التراث تلقائياً
- حفظ سجل المراجعة (ReviewLog)

## 📊 الملفات المعدلة

1. ✅ `app/dashboard/games/new/page.tsx` - تحديث كامل
2. ✅ `lib/actions/helpers.ts` - ملف جديد للدوال المساعدة
3. ✅ `GAME_CREATION_FIX.md` - توثيق التفصيلي بالإنجليزية

## 🎉 النتيجة

الآن يمكنك إضافة ألعاب جديدة وسيتم حفظها بنجاح في قاعدة البيانات! 🎮✨
