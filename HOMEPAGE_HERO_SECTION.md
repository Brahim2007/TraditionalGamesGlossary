# 📺 قسم Hero في الصفحة الرئيسية

## ✅ المكونات الموجودة

### 1️⃣ الفيديو
- ✅ موجود في `HeroVideoPlayer` component
- ✅ يعرض مقاطع فيديو من YouTube
- ✅ يتم تحميل الإعدادات من قاعدة البيانات

### 2️⃣ العنوان والوصف
```
ذاكرة الألعاب الشعبية
منصة توثيقية تفاعلية تحفظ قواعد وقيم الألعاب التقليدية في العالم العربي، 
لتبقى جسراً للأجيال القادمة.
```

### 3️⃣ الإحصائيات (تم تحديثها)
- ✅ عدد الألعاب الكلي (14)
- ✅ عدد الأقاليم
- ✅ عدد الدول

---

## 🔍 لماذا قد لا يظهر الفيديو؟

### السبب 1: إعدادات الفيديو في قاعدة البيانات
الفيديو يتم تحميله من جدول `Settings` في قاعدة البيانات.

**الحل:**
```sql
-- تحقق من وجود الإعداد
SELECT * FROM "Settings" WHERE key = 'hero_video_clips';

-- إذا لم يكن موجوداً، أضفه
INSERT INTO "Settings" (id, key, value, category, label)
VALUES (
  'hero_video_1',
  'hero_video_clips',
  '[{"videoId":"k-vNeRMMILo","startTime":0,"endTime":30,"title":"مقطع 1"}]',
  'homepage',
  'مقاطع فيديو الصفحة الرئيسية'
);
```

### السبب 2: YouTube API لم يتم تحميله
الفيديو يستخدم YouTube IFrame API.

**الحل:** تأكد من اتصال الإنترنت وأن YouTube غير محجوب.

### السبب 3: التنسيقات (CSS) لم يتم تحميلها
قد تكون الـ animations لم تُحمل بشكل صحيح.

**الحل:**
```bash
# امسح ذاكرة التخزين المؤقت
rm -rf .next
npm run build
npm run dev
```

---

## 🎯 التحقق من المشكلة

### 1. افتح المتصفح
```
http://localhost:3001
```

### 2. افتح Developer Tools (F12)
- اذهب إلى **Console**
- ابحث عن أخطاء JavaScript

### 3. تحقق من Network
- اذهب إلى **Network** tab
- تأكد من تحميل:
  - `https://www.youtube.com/iframe_api`
  - جميع ملفات CSS

---

## 🔧 الحلول السريعة

### الحل 1: إعادة بناء المشروع
```bash
# أوقف الخادم (Ctrl+C)
npm run build
npm run dev
```

### الحل 2: مسح ذاكرة التخزين المؤقت
```bash
rm -rf .next
npm run dev
```

### الحل 3: التحقق من قاعدة البيانات
```bash
# افتح Prisma Studio
npm run db:studio

# اذهب إلى Settings
# ابحث عن key = 'hero_video_clips'
```

### الحل 4: إضافة الفيديو يدوياً
إذا لم يكن موجوداً في قاعدة البيانات:

1. افتح `/dashboard/settings`
2. أضف إعداد جديد:
   - **Key:** `hero_video_clips`
   - **Value:** 
     ```json
     [{"videoId":"k-vNeRMMILo","startTime":0,"endTime":30,"title":"مقطع 1"}]
     ```
   - **Category:** `homepage`
   - **Label:** `مقاطع فيديو الصفحة الرئيسية`

---

## 📸 ما يجب أن تراه

### في الصفحة الرئيسية:

```
┌─────────────────────────────────────────────────────┐
│  [الفيديو في شكل اقتباس]  │  العنوان والوصف الكبير │
│                             │  ذاكرة الألعاب الشعبية │
│  [YouTube Player]           │  منصة توثيقية...       │
│                             │  [أزرار: استكشف، ابحث]  │
└─────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│  14          3           X                           │
│  لعبة موثقة  أقاليم      دولة                       │
│  (6 منشورة)                                         │
└──────────────────────────────────────────────────────┘
```

---

## 🆘 إذا استمرت المشكلة

1. **أرسل لقطة شاشة** من الصفحة الرئيسية
2. **أرسل أخطاء Console** (F12 → Console)
3. **تحقق من قاعدة البيانات:**
   ```bash
   npm run db:studio
   # افتح Settings وابحث عن hero_video_clips
   ```

---

**الخادم يعمل على:** `http://localhost:3001` 🚀
